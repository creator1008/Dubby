# Dubby worker (media pipeline) dependencies.
# Install with the CPU wheel index on servers without a GPU:
#   pip install --extra-index-url https://download.pytorch.org/whl/cpu -r requirements-worker.txt
torch>=2.2,<3
torchaudio>=2.2,<3
demucs==4.0.1
# Optional at runtime; loaded only when DIARIZATION_PROVIDER=pyannote.
pyannote.audio>=3.3,<4
