# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Phase 0 waitlist local store
/data/

# Local pipeline scratch (manifests only; media lives in R2)
.local-data/

# Accidental nested clone / worktree
/dubby/

# Python API
api/.venv/
api/data/
api/fb-cookies.txt
api/cookies.txt
__pycache__/
*.pyc
.pytest_cache/

# Cloudflare named tunnel local config (paths + tunnel id)
infra/cloudflared/config.yml

# Cloudflare named tunnel (contains local paths)
infra/cloudflared/config.yml
